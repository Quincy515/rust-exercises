[package]
edition = "2021"
name = "my-serde"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["serde_std"]
derive = ["serde_derive"]
serde_std = []
serde_derive = []

[dependencies]
